import React from "react";
import type { variant, size, color, ripple, className, children } from "../../types/components/button";
export interface IconButtonProps extends React.ComponentProps<"button"> {
    variant?: variant;
    size?: size;
    color?: color;
    ripple?: ripple;
    className?: className;
    children: children;
    fullWidth?: boolean;
}
export declare const IconButton: React.ForwardRefExoticComponent<Omit<IconButtonProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
export default IconButton;
//# sourceMappingURL=index.d.ts.map